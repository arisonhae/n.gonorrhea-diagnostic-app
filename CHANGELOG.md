# 변경 이력

제출된 논문·포스터(2025년 11월, 성균관대학교 생명공학대학 교내 경진대회)는
그대로 두고, 이후 확인된 사항과 코드 변경만 여기에 기록한다.

따라서 **이 저장소의 값이 논문과 다른 부분이 있으며**, 어느 쪽이 어떤 근거인지는
아래에서 확인할 수 있다.

---

## 2026-08 · 코드 정리 및 재검증

제출 약 9개월 후, 코드를 다시 읽고 결과를 재현하면서 정리한 내용이다.

전체 파이프라인(step1~4)을 다시 실행해 논문의 값이 재현되는지 확인했고,
그 과정에서 버그 세 건과 잘못된 서술 여러 건을 발견했다.
새로 수행한 검증에서는 논문에 없던 수치도 나왔다.

---

## 1. 값이 바뀐 것

### 판정 임계값: 1.148 → 1.1162

`RATIO_THR` 은 solo 데이터에서 얻은 절대 기준선을 pair 좌표계로 옮긴 값이다.

```
T_ratio = step3_cutoff / median(pair NC 형광값)
```

원본은 `pair/neg_pos` 20장만 사용했다.

```
221.0 / 192.50 = 1.1481
```

이후 `pair/neg_neg` 26장을 추가로 촬영했는데, 이 이미지들의 위쪽 튜브도
NC 이므로 기준 분포에 포함할 수 있다. 원본 시점에는 이 데이터가
아직 없었기 때문에 반영되지 않았다.

```
221.0 / 198.00 = 1.1162   (n=20 → n=44)
```

**근거**

부트스트랩 2000회로 신뢰구간을 산출한 결과, 표본을 늘렸을 때 추정이 훨씬
안정적이었다.

| 조건 | n | T_ratio | 95% CI | 구간 폭 |
|---|---|---|---|---|
| neg_pos 만 (원본) | 20 | 1.1481 | [1.1078, 1.2738] | 0.166 |
| **neg_pos + neg_neg** | **44** | **1.1162** | **[1.0914, 1.1571]** | **0.066** |
| Youden's J 직접 최적화 | 44 | 1.1024 | [1.0867, 1.1980] | 0.111 |

pair 데이터 자체에서의 성능도 낮은 임계값이 나았다.

| 임계값 | TP | FN | FP | TN | 정확도 |
|---|---|---|---|---|---|
| 1.1481 | 18 | **2** | 4 | 20 | 86.4% |
| **1.1162** | **19** | **1** | 4 | 20 | **88.6%** |

위양성 수는 같고 위음성만 줄어든다. 진단에서 감염자를 놓치는 쪽이 더 위험하므로
낮은 임계값이 적절하다.

기기 호환성 검증(test_all, 13장)에서는 두 값 모두 12/13 으로 차이가 없었다.
표본이 작아 임계값 선택의 근거가 되지 못한다.

### 절대 기준선 221.0 은 유지

바꾸는 것을 검토했으나 유지하기로 했다.

| 방식 | T | 95% CI | test 결과 |
|---|---|---|---|
| **99.7 백분위수 (원본)** | **221.000** | [213.88, 221.00] | TP15 FN0 FP1 TN14 · 96.7% |
| mean + 3SD | 240.627 | — | TP11 FN4 FP0 TN15 · 86.7% |
| Youden's J | 221.500 | [217.00, 223.00] | TP15 FN0 FP1 TN14 · 96.7% |

n=40 에서 99.7 백분위수는 최댓값과 같아지므로, 가장 밝은 음성 이미지 한 장이
기준선을 결정하는 구조인 것은 사실이다. 그러나 양성 정보까지 활용하는
Youden's J 로 재산출한 값이 221.5 로 거의 같았고 test 성능도 동일했다.
방법을 바꿀 근거가 없다.

---

## 2. 새로 확인된 사항

### 위양성률 16.7%

`pair/neg_neg` 24장 중 4장이 양성으로 오판된다(95% CI 6.7 – 35.9%).
Youden's J 로 임계값을 최적화해도 FPR 은 16.7% 로 동일하다. 즉 **임계값
조정으로는 더 낮출 수 없으며, 음성-음성 쌍과 음성-양성 쌍의 ratio 분포가
실제로 겹쳐 있다.**

오판된 네 장은 모두 상단 NC 의 형광값이 중앙값(202.5)보다 낮았다.

| 이미지 | ratio | I_nc | I_sample |
|---|---|---|---|
| neg_neg_21 | 1.1553 | 161.0 | 186.0 |
| neg_neg_22 | 1.1749 | 183.0 | 215.0 |
| neg_neg_23 | 1.2174 | 184.0 | 224.0 |
| neg_neg_24 | 1.1889 | 180.0 | 214.0 |

예외가 없다. 시료가 밝아서가 아니라 기준 튜브가 어둡게 찍혀 생긴 오판이다.
검출 결과 이미지를 확인한 결과 ROI 검출 자체는 정상이었다.

임계값을 올려도 해결되지 않는다.

| T | 위양성 | 위음성 |
|---|---|---|
| **1.1162 (현재)** | **4/24** | **1/20** |
| 1.2000 | 1/24 | 5/20 |
| 1.2175 | 0/24 | 9/20 |

근본 원인은 측정 변동이다. 같은 음성 시료라도 두 튜브의 측정값이 ±9% 정도
흔들리며(SD 0.089), 그 변동이 판정 마진(약 11.6%)과 비슷한 크기다.

이 수치는 논문에 없다. `neg_neg` 데이터가 제출 이후 촬영된 것이기 때문이다.

### AUC — 비율 정규화의 대가

임계값에 의존하지 않는 지표를 새로 산출했다.

| 데이터 | AUC | 95% CI |
|---|---|---|
| solo train (G_p95 절댓값) | 0.9994 | 0.9972 – 1.0000 |
| pair (ratio) | 0.9646 | 0.9027 – 0.9979 |

절댓값으로는 거의 완전히 갈리지만 비율로 바꾸면 성능이 떨어진다. 비율은 기기
차이를 상쇄해 주는 대신 NC 튜브의 변동을 새로 끌어들이기 때문이다.
기기별 편차가 19% 에 이르므로 그 대가를 치를 만하지만, 대가가 있다는 점은
분명히 해 둘 필요가 있다.

### 검출 모델의 기기 편향

기기 호환성 검증 이미지 22장 중 9장이 ROI 검출에 실패했다. 그중 4장은
의도적으로 촬영한 오류 이미지지만 나머지는 정상 이미지다.

통과하지 못한 3장은 ROI 를 2개 모두 검출했으나 두 번째 ROI 의 confidence 가
0.656~0.678 로 임계값(0.70)에 근소하게 미달했다. 3장 모두 Galaxy Note 8 로
촬영한 것이며, 기기별 분포는 다음과 같다.

| 기기 | second_roi_conf |
|---|---|
| Galaxy Note 8 | 0.656 ~ 0.793 |
| iPhone 13 | 0.801 ~ 0.815 |
| iPhone 13 Pro | 0.797 ~ 0.804 |

임계값을 낮춰 덮기보다 학습 데이터에 해당 기기 이미지를 보강해 재학습하는 것이
근본 해결이다. 참고로 pilot 단계에서도 Galaxy Note 8 의 음성 배경 신호가
12% 높게 나온 바 있어, 일관된 관찰이다.

### 포화

test set 양성 15장 중 5장이 G_p95 = 255 로 포화되었다. 모두 촬영 조건 최적화
실험에서 나온 이미지(blue, 2x, r_0)다. 포화된 값은 무조건 기준선을 넘으므로
사실상 자동 정답이 된다.

기본 조건(tube_*) 20장만으로 계산하면 정확도는 96.7% 가 아니라 95.0% 다.

임계값 산출에 쓰인 train 80장과 pair 44장에는 포화가 없다. 다만 train 양성의
최댓값이 254 로 상한에 근접해 있어, 조명이 조금만 더 강해도 측정이 불가능해진다.

### 학습 데이터 분할 — 문제 없음

한때 leakage 를 의심했으나, 확인 결과 문제가 없었다.

현재 모델의 학습 데이터셋은 Roboflow `hajamboree/tube_detection-thlv6` **v8**
(2025-11-04, 700장) 이다. 원본 300개를 train 200 / valid 50 / test 50 으로
나눈 뒤 train 에만 증강(회전 ±15°, 밝기 ±15%)을 3배 적용해 600장을 만들었다.
600 + 50 + 50 = 700 으로 맞는다.

동일 원본이 여러 split 에 들어간 사례는 없었다. 파일명 앞부분이 겹치는 경우가
있으나 서로 다른 이미지다. 두 촬영 기기(iPhone 13 Pro, Galaxy Note 8)에서
같은 파일명 규칙을 썼기 때문이며, **파일명만으로 원본 수를 세면 실제보다 적게
집계된다.** train 600장을 원본명 기준으로 묶으면 98개가 3장씩, 51개가 6장씩
나오는데, 6장짜리는 증강 3배 × 기기 2대에 해당한다. 따라서 실제 원본은
98 + 51×2 = 200장이다.

한편 초기 버전 v2(2025-11-02, 441장, 1클래스)는 로컬에 남아 있는데,
이 버전에서는 증강 후 랜덤 분할이 이루어져 train 과 test 에 8개, train 과
valid 에 5개 원본이 중복된다. 이 버전으로 학습한 것은 `weights.pt`(1클래스)이며
현재 배포 모델과 무관하다.

---

## 3. 문서 오류 정정

**포스터의 `µ+3σ` 표기는 잘못되었다.**

step3 의 정규성 검정 결과 Shapiro-Wilk p < 0.0001 로 정규분포가 기각되므로,
코드는 99.7 백분위수를 사용한다. 두 값은 실제로 다르다.

```
mean + 3SD = 240.627
p99.7      = 221.000   ← 실제 사용된 값
```

논문의 `99.7 percentile` 표기가 맞다.

**모델은 YOLOv8n 이 아니라 YOLOv8s 다.**

가중치 메타데이터에서 확인된 학습 설정이다.

```
model     : yolov8s.pt
epochs    : 300 (patience=90 → 213 epoch 에서 조기 종료)
imgsz     : 640
seed      : 0
version   : ultralytics 8.3.171
date      : 2025-11-04
```

논문과 포스터에 기재된 "YOLOv8n(nano)" 및 nano 선택 이유는 사실과 다르다.
파일 크기도 21.5MB 로, YOLOv8n(약 6MB)이 아닌 YOLOv8s(약 22MB)에 해당한다.

또한 "213 epoch 학습"은 지정한 값이 아니라 early stopping 결과다.
300 epoch 설정에 patience 90 이었고, 123 epoch 에서 best 가 나온 뒤
90 epoch 동안 개선이 없어 중단되었다.

**검출 모델의 클래스는 2개다.**

배포된 가중치의 클래스는 `roi`, `tube` 두 개다. 논문에 기재된 3클래스
(`tube`, `roi_negative`, `roi_positive`) 구성이 아니다.

따라서 검출 모델이 양성/음성을 판별하지 않으며, 판정은 전적으로
ROI 의 형광값 비율로 이루어진다.

**검출 성능 수치가 다르다.**

Roboflow 가 보고한 v8 모델의 성능은 mAP@50 99.9%, Precision 99.1%,
Recall 100%, F1 99.6% 다. 논문에 기재된 mAP@50 99.0% 와 다르다.

**촬영 조건 실험의 표본 수.**

논문에는 조건당 표본이 명시되지 않은 부분이 있으나, 실제로는 조건당 2~20장이다.
다만 대부분의 조건에서 통계적 유의성을 확보하지 못했다.

---

## 4. 코드 수정

### 버그

**step1 — 실행 자체가 되지 않았다**

CLI 스크립트에 streamlit 코드가 섞여 있어, import 없이 `st.markdown` 을
호출하며 즉시 `NameError` 로 중단되었다. 해당 블록을 제거했다.

**step2a — pair 이미지의 아래쪽을 무조건 양성으로 라벨링**

`neg_neg` pair 를 넘기면 음성 튜브가 양성으로 잘못 들어간다.
원본 실행 명령은 solo 만 넘겼으므로 결과에는 영향이 없었으나, 방어 장치를
추가했다.

**step4 — 위쪽 튜브 판정이 하드코딩**

```python
up, lp = ("neg", "pos") if is_pos else ("neg", "neg")
```

`upper_pred` 가 항상 `"neg"` 로 고정되어, 위쪽 튜브 정확도가 의미 없는 값이었다.
절대 기준으로 실제 판정하도록 수정했다.

다만 `test_all` 에 위쪽이 양성인 이미지가 없어, 수정 후에도 이 판정은
검증되지 않은 상태다.

**step4 — 최종 검증 데이터로 파라미터를 선택**

보정 방식(none / ratio / shift / affine)을 모두 시도한 뒤 `test_all` 정확도가
가장 높은 것을 채택하는 구조였다. 최종 검증용 데이터로 하이퍼파라미터를
고르면 성능이 낙관적으로 평가된다.

임계값 도출은 pair 데이터에서만 수행하고, `test_all` 은 평가 전용으로 분리했다.

**app — 오타로 앱이 죽어 있었다**

`RATIO_THR` 이 `RRATIO_THR` 로 잘못 적혀 있어 판정 시점에 `NameError` 가
발생했다. 배포된 앱도 같은 상태였다.

### 통계 보강

- 임계값에 부트스트랩 신뢰구간 추가
- 모든 정확도 수치에 Wilson 신뢰구간 추가
- Welch t-test 와 함께 Mann-Whitney U 수행 (정규성이 기각되므로)
- Cohen's d 와 함께 Cliff's delta 산출
- 포화(G채널 255) 추적

정규성은 대부분의 조합에서 성립하지 않았으나, 모수·비모수 검정의 결론이
일치하므로 가정 위반이 결과를 뒤집지는 않았다.

### 앱 안전 로직

진단 도구에서는 잘못된 판정보다 판정 불가가 안전하다는 원칙에 따라,
아래 상황에서 결과를 내지 않도록 했다. 기존에는 경고만 표시하고 판정을
그대로 출력했다.

| 상황 | 이유 |
|---|---|
| ROI 두 개가 검출되지 않음 | 비율 계산 불가 |
| 상단 밝기가 221.0 초과 | 위쪽이 NC 가 아닐 수 있음 |
| 어느 한쪽 ROI 가 포화 | 실제 형광 세기를 알 수 없음 |
| 튜브가 3개 이상 검출 | 의도한 조합인지 확인 필요 |

특히 튜브 순서가 뒤바뀐 경우 ratio 가 1 미만이 되어 **음성으로 표시**되므로,
경고만 띄우면 위음성이 그대로 나간다.

판정이 나오더라도 ratio 가 임계값에서 0.05 이내면 경계값으로 표시하고
재촬영을 권한다.

Gemini API 오류도 사용자가 읽을 수 있는 문장으로 바꿨다. 할당량 초과 시
내부 메시지가 그대로 노출되고 있었다.

### 실행 환경

- 하드코딩된 절대 경로를 모두 `paths.py` 로 이관
- 이미지 데이터를 코드 저장소와 분리하고, 환경변수 `NGD_DATA_ROOT` 로 위치를
  지정하도록 정리했다. 미설정 시 저장소 내 `data/` 를 본다.
  (`analysis/pilot/` 의 pilot 스크립트는 아직 절대 경로가 남아 있다.)
- `st.secrets` 접근을 감싸 `secrets.toml` 없이도 앱이 실행되도록 수정
- 검출 오버레이 이미지 저장을 기본 끄기로 변경 (PNG 원본 크기로 저장되어
  산출물이 약 2GB 에 달했다). 저장 시에도 JPEG 로 리사이즈한다.

---

## 남은 과제

- `qc_test` 이미지가 대부분 단일 튜브로 촬영되어 pair 기반 QC 성능을 평가할 수
  없다. 특히 흐린 pair 이미지가 하나도 없어, 초점이 맞지 않은 이미지가 검출을
  통과해 잘못된 판정을 내는지 확인되지 않았다.
- 위쪽 튜브 QC 가 절대 기준 하나에 의존한다. 위쪽에 양성을 놓은 이미지에서
  상단 밝기 217 이 경고 기준 221.0 에 미달해 안전장치가 작동하지 않았다.
  정상 음성 쌍의 상단이 202~203 이므로 기준을 낮추면 정상 이미지까지 걸린다.
  검증에 쓸 수 있는 `pos_half` 이미지 2장(Galaxy Note 8, iPhone 13 Pro)이
  `final_test/test_all` 에 있으나 평가 세트인 `dataset/test_all` 에는 포함되지
  않았다. step4 는 `pos_half` 를 (위 pos, 아래 pos)로 처리하는 로직을 이미
  갖고 있으므로, 두 장을 추가하면 이 경로를 평가할 수 있다.
- 검출 모델의 기기 편향을 해소하려면 Galaxy Note 8 이미지를 보강해
  재학습해야 한다.
- 위양성률 16.7% 는 신호 분포의 겹침에서 오므로, 두 튜브가 같은 조명 조건에
  놓이도록 촬영 방식을 개선하거나 표본을 늘려야 한다.