# 변경 이력

제출된 논문·포스터(2025년 11월, 성균관대학교 생명공학대학 교내 경진대회)는
그대로 두고, 이후 확인된 사항과 코드 변경만 여기에 기록한다.

따라서 **이 저장소의 값이 논문과 다른 부분이 있으며**, 어느 쪽이 어떤 근거인지는
아래에서 확인할 수 있다.

---

## 2026-08 · 코드 정리 및 재검증

제출 약 9개월 후, 코드를 다시 읽고 결과를 재현하면서 정리한 내용이다.

### 판정 임계값 변경: 1.148 → 1.1162

**변경 이유**

`RATIO_THR` 은 solo 데이터에서 얻은 절대 기준선을 pair 좌표계로 옮긴 값이다.

```
T_ratio = step3_cutoff / median(pair NC 형광값)
```

원본은 `pair/neg_pos` 20장만 사용했다.

```
221.0 / 192.50 = 1.1481
```

이후 `pair/neg_neg` 26장을 추가로 촬영했는데(2025-11-17), 이 이미지들의
위쪽 튜브도 NC 이므로 기준 분포에 포함할 수 있다. 원본 시점에는 이 데이터가
아직 없었기 때문에 반영되지 않았다.

```
221.0 / 198.00 = 1.1162   (n=20 → n=44)
```

**근거**

부트스트랩 2000회로 신뢰구간을 산출한 결과, 표본을 늘렸을 때 추정이 훨씬
안정적이었다.

| 조건 | n | T_ratio | 95% CI | 구간 폭 |
|---|---|---|---|---|
| neg_pos 만 (원본) | 20 | 1.1481 | [1.1078, 1.2738] | 0.166 |
| **neg_pos + neg_neg** | **44** | **1.1162** | **[1.0914, 1.1571]** | **0.066** |
| Youden's J 직접 최적화 | 44 | 1.1024 | [1.0867, 1.1980] | 0.111 |

pair 데이터 자체에서의 성능도 낮은 임계값이 나았다.

| 임계값 | TP | FN | FP | TN | 정확도 |
|---|---|---|---|---|---|
| 1.1481 | 18 | **2** | 4 | 20 | 86.4% |
| **1.1162** | **19** | **1** | 4 | 20 | **88.6%** |

위양성 수는 같고 위음성만 줄어든다. 진단에서 감염자를 놓치는 쪽이 더 위험하므로
낮은 임계값이 적절하다.

기기 호환성 검증(test_all, 13장)에서는 두 값 모두 12/13 으로 차이가 없었다.
표본이 작아 임계값 선택의 근거가 되지 못한다.

---

### 새로 확인된 사항

**위양성률 16.7%**

`pair/neg_neg` 24장 중 4장이 양성으로 오판된다. Youden's J 로 임계값을
최적화해도 FPR 은 16.7% 로 동일하다. 즉 **임계값 조정으로는 더 낮출 수 없으며,
음성-음성 쌍과 음성-양성 쌍의 ratio 분포가 실제로 겹쳐 있다.**

논문에는 이 수치가 없다. `neg_neg` 데이터가 제출 이후에 촬영된 것이기 때문이다.

**정확도 신뢰구간**

논문의 96.7% 는 solo test 30장 기준이며, Wilson 95% 신뢰구간은 **83.3–99.4%**
이다. 표본이 작아 구간이 넓다는 점이 함께 제시되어야 한다.

**절대 기준선의 불안정성**

step3 의 cutoff 221.0 은 train 음성 40장의 99.7 백분위수인데, n=40 에서
99.7 백분위수는 최댓값과 같아진다. 실제로 train 음성의 최댓값이 정확히 221.0 이다.
**단일 극단값이 기준선 전체를 결정하는 구조다.**

**기기 간 NC 형광값 차이**

| 기기 | NC 평균 |
|---|---|
| Galaxy Note 8 | 209.2 |
| iPhone 13 | 201.7 |
| iPhone 13 Pro | 176.0 |

최대 약 19% 차이로, 판정 마진(약 11.6%)보다 크다. 절댓값으로 판정했다면
iPhone 13 Pro 는 전부 음성으로 나왔을 것이다. **비율 정규화를 쓰는 이유가
데이터로 확인된 셈이다.** 다만 위쪽 튜브의 QC 경고는 여전히 절대 기준(221.0)을
쓰므로 기기에 따라 오작동할 수 있다.

---

### 문서 오류 정정

**포스터의 `µ+3σ` 표기는 잘못되었다.**

step3 의 정규성 검정 결과 Shapiro-Wilk p < 0.0001 로 정규분포가 기각되므로,
코드는 99.7 백분위수를 사용한다. 두 값은 실제로 다르다.

```
mean + 3SD = 240.627
p99.7      = 221.000   ← 실제 사용된 값
```

논문의 `99.7 percentile` 표기가 맞다.

**모델은 YOLOv8n 이 아니라 YOLOv8s 다.**

가중치 메타데이터에서 확인된 학습 설정이다.

```
model     : yolov8s.pt
epochs    : 300 (patience=90 → 213 epoch 에서 조기 종료)
imgsz     : 640
seed      : 0
```

논문과 포스터에 기재된 "YOLOv8n(nano)" 및 nano 선택 이유는 사실과 다르다.
파일 크기도 21.5MB 로, YOLOv8n(약 6MB)이 아닌 YOLOv8s(약 22MB)에 해당한다.

또한 "213 epoch 학습"은 지정한 값이 아니라 early stopping 결과다.
300 epoch 설정에 patience 90 이었고, 123 epoch 에서 best 가 나온 뒤
90 epoch 동안 개선이 없어 중단되었다.

**검출 모델의 클래스는 2개다.**

배포된 가중치의 클래스는 `roi`, `tube` 두 개다. 논문에 기재된 3클래스
(`tube`, `roi_negative`, `roi_positive`) 구성이 아니다.

따라서 검출 모델이 양성/음성을 판별하지 않으며, 판정은 전적으로
ROI 의 형광값 비율로 이루어진다.

---

### 코드 수정

**step4 — 위쪽 튜브 판정이 하드코딩되어 있었다**

```python
up, lp = ("neg", "pos") if is_pos else ("neg", "neg")
```

`upper_pred` 가 항상 `"neg"` 로 고정되어, 위쪽 튜브 정확도가 의미 없는 값이었다.
절대 기준으로 실제 판정하도록 수정했다.

다만 `test_all` 에 위쪽이 양성인 이미지가 없어, 수정 후에도 이 판정은
검증되지 않은 상태다.

**step4 — 최종 검증 데이터로 파라미터를 선택하고 있었다**

보정 방식(none / ratio / shift / affine)을 모두 시도한 뒤 `test_all` 정확도가
가장 높은 것을 채택하는 구조였다. 최종 검증용 데이터로 하이퍼파라미터를
고르면 성능이 낙관적으로 평가된다.

임계값 도출은 pair 데이터에서만 수행하고, `test_all` 은 평가 전용으로 분리했다.

**step1 — 실행 자체가 되지 않았다**

CLI 스크립트에 streamlit 코드가 섞여 있어, import 없이 `st.markdown` 을
호출하며 즉시 `NameError` 로 중단되었다. 해당 블록을 제거했다.

**공통 — 경로 하드코딩 제거**

모든 스크립트가 특정 PC 의 절대 경로를 사용하고 있어 다른 환경에서
실행할 수 없었다. `paths.py` 로 이관했다.

**공통 — 검출 오버레이 저장 방식**

PNG 원본 크기로 저장되어 산출물이 약 2GB 에 달했다. 기본적으로 저장하지 않도록
바꾸고, 저장 시에도 JPEG 로 리사이즈한다.

---

## 남은 과제

- YOLO 데이터셋 분할이 이미지 단위로 이루어져 있어, 같은 튜브를 여러 각도로
  촬영한 이미지가 train 과 test 에 나뉘어 들어갔을 가능성이 있다.
  시료 단위 재분할 후 검출 성능 재측정이 필요하다.
- `test_all` 22장 중 9장이 ROI 검출에 실패했다. 그중 4장은 의도적으로 촬영한
  오류 이미지지만, 나머지는 정상 이미지다. 검출 confidence 임계값(0.70)이
  높아서인지 확인이 필요하다.
- 앱에서 튜브 방향이 뒤집힌 경우 경고만 표시되고 판정은 그대로 출력된다.
  판정 불가로 처리하는 편이 안전하다.
- qc_test 이미지가 대부분 단일 튜브로 촬영되어 pair 기반 QC 성능을 평가할 수
  없다. 특히 흐린 pair 이미지에 대한 검증이 전혀 없다.